function drawBackground()
{
  // canvas的坐标,左上角为原点
  ctx2.drawImage(bgPic,0,0,canWidth,canHeight);//drawImage（）方法将原图片的像素复制在画布上，
  /**
   * 如果三个参数
   * 第一个参数是原图片
   * 第二个参数是待绘制图片的X轴坐标
   * 第3个参数是待绘制图片的Y轴坐标
   * 如果5各参数
   * 第四个参数是宽度
   * 第五个参数是高度
   * 如果九个参数
   * 第二到五个参数制定了原矩形区域，且只复制矩形区域内的像素，第六道九个参数制定了图片要绘制的目标区域
   */
}
